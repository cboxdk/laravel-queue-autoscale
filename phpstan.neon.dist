parameters:
    level: max
    paths:
        - src
        - config
        # Composition sites for the traits shipped in src/Testing, which Pest's
        # uses() hides from analysis.
        - tests/Fixtures
    tmpDir: build/phpstan
    checkOctaneCompatibility: true
    checkModelProperties: true
    noEnvCallsOutsideOfConfig: false
